#!/usr/bin/env python

import sys

sam1 = open(sys.argv[1])
sam2 = open(sys.argv[2])

#Check genomes are the same

#Parse map/unmapped
unmapped = 0
count    = 0

while True:
	line = sam1.readline()
	if line = "":
		break
	if not line.startswith("@"):
		flag = int(line.split()[1])
		if flag & 4:
			unmapped += 1
		count += 1

pcnt_unmapped = float(unmapped)/count